# joytouchUSB
connect USB(by android open accessory protocol) with joytouch(https://github.com/hmatsumoto000dev/joytouch).
windows application implemented by Node.js

## ビルド方法

実行形式のファイル（.exe）をビルドするには、以下のコマンドを実行します。

```bash
npx pkg index.js --targets node18-win-x64 -o dist/joytouchUSB.exe
```
